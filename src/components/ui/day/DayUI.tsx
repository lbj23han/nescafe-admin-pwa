"use client";

import { Layout, Main, Section } from "./ui/DayLayout";
import {
  HeaderRow,
  BackButton,
  HeaderTitleBlock,
  Header,
} from "./ui/DayHeader";
import { Label, Field, TextInput } from "./ui/DayFormControls";
import {
  PrimaryButton,
  FooterRow,
  StatusText,
  ActionGroup,
  ActionButton,
} from "./ui/DayButtons";
import {
  ReservationCard,
  ReservationTitle,
  MetaText,
  EditSection,
} from "./ui/DayReservationCard";
import {
  SheetOverlay,
  SheetContainer,
  SheetHeader,
  SheetTitle,
  SheetDesc,
  SheetCloseButton,
  SheetFooter,
  SheetActionButton,
} from "./ui/DaySheet";

export const DayUI = {
  Layout,
  Main,
  Section,

  HeaderRow,
  BackButton,
  HeaderTitleBlock,
  Header,

  Label,
  Field,
  TextInput,

  PrimaryButton,
  FooterRow,
  StatusText,
  ActionGroup,
  ActionButton,

  ReservationCard,
  ReservationTitle,
  MetaText,
  EditSection,

  SheetOverlay,
  SheetContainer,
  SheetHeader,
  SheetTitle,
  SheetDesc,
  SheetCloseButton,
  SheetFooter,
  SheetActionButton,
};
